# 🎨 Centralized Presentation System

## 📋 Система карты знаний (Knowledge Map)

### 🏗️ Интеграция в build-presentation.sh

Система карты знаний автоматически интегрируется в HTML-презентации через `build-presentation.sh`. При наличии концептов (`concept_*` классов) в презентации автоматически:

1. **Копируется `knowledge-map-new.js`** из .themes в generated/
2. **Настраивается фоновое изображение** для hex-карты
3. **Подключаются стили и скрипты** к HTML файлу
4. **Создается интерактивная навигация** с hex-картой и mindmap

### 🎯 Архитектура системы

**Основные компоненты:**
- **Hex-карта** - сотовая структура с концептами для визуального отслеживания прогресса
- **Mindmap** - иерархическая схема связей концептов с анимацией
- **Умная навигация** - возврат к исходному слайду после изучения концепта
- **Персистентность** - сохранение прогресса в localStorage

**Алгоритм работы:**
1. **Сканирование слайдов** - автоматическое извлечение `concept_*` классов
2. **Построение структуры** - создание иерархии parent-child из названий концептов
3. **Распределение hex-ячеек** - случайное размещение концептов по сотовой структуре
4. **Отслеживание прогресса** - активация концептов при навигации по слайдам
5. **Синхронизация состояния** - обновление hex-карты и mindmap в реальном времени

### 🔧 Техническая реализация

**Файловая структура:**
```bash
.themes/
├── knowledge-map-new.js     # ← Основная логика карты знаний
├── build-presentation.sh   # ← Автоматическая интеграция
└── CLAUDE.md               # ← Эта документация

# При сборке создается:
generated/
├── knowledge-map-new.js    # ← Копия с настроенным фоном
├── resources/
│   └── image_kubernetes.png # ← Фоновое изображение
└── presentation.html       # ← С подключенными скриптами
```

**Ключевые функции в knowledge-map-new.js:**
- `updateProgressUpToSlide()` - двойной режим прогресса (аккумулятивный/синхронный)
- `returnToMap()` - умный возврат к исходному слайду с открытием карты
- `updateMindmap()` - синхронизация состояния hex-карты и mindmap
- `normalizeTag()` - преобразование ключей концептов для согласованности

### 🎮 Функциональность UX

**Автоматическое поведение:**
- **Кнопка "Карта знаний"** появляется только на слайдах `page_question`
- **Прогресс-бар** показывает процент изученных концептов
- **Hex-ячейки** открываются по мере прохождения слайдов с концептами
- **Mindmap узлы** становятся зелеными при изучении концептов

**Интерактивная навигация:**
- **Клик по hex-метке** → переход к первому слайду концепта
- **Клик по mindmap узлу** → навигация к объясняющему слайду
- **Автоматическая кнопка возврата** → появляется в левом верхнем углу после перехода из карты
- **ESC** → закрытие полноэкранной карты
- **Стрелки (в карте)** → переключение между Hex ↔ Mindmap режимами

**Умная система возврата:**
```javascript
// Логика работы кнопки "Вернуться к карте знаний"
1. Клик по концепту в карте → сохранение исходного слайда + флаг навигации
2. Карта закрывается → переход к слайду концепта → появление кнопки через 300ms
3. Клик по кнопке возврата → возврат к исходному слайду + открытие карты
4. Ручная навигация (←/→) → автоскрытие кнопки + сброс флага
5. Автоскрытие через 8 секунд → плавное исчезновение
```

### 📝 Разметка концептов в презентациях

**Правильное размещение концептов:**

✅ **Размещаем на объясняющих слайдах:**
```markdown
---
<!-- _class: page concept_authorization-rbac$RBAC -->
# Как работает RBAC

ДЕТАЛЬНОЕ объяснение принципов RBAC:
- Role-Based Access Control
- Связывание пользователей с ролями
- Проверка прав доступа
```

❌ **НЕ размещаем на обзорных слайдах:**
```markdown
---
<!-- _class: page_section concept_authorization$AUTH -->
# Авторизация  <!-- Только упоминание темы -->

---
<!-- _class: page concept_rbac$RBAC -->
# Виды авторизации
- RBAC  <!-- Список без объяснения -->
- ABAC
```

**Правила именования концептов:**
- **Базовый концепт**: `concept_authentication$AUTH`
- **Иерархический**: `concept_authorization-rbac$RBAC`
- **Многословный**: `concept_network-policy$NP`
- **Глубокая вложенность**: `concept_authorization-rbac-roles$ROLES`

### 🔄 Решенные технические проблемы

**1. Проблема ключей mindmap (исправлено)**
- **Было**: узлы создавались с пробелами, но `revealedTags` содержал подчеркивания
- **Решение**: унификация ключей с подчеркиваниями во всех компонентах системы

**2. Деструктивный прогресс (исправлено)**
- **Было**: при возврате из карты удалялись "будущие" концепты
- **Решение**: двойной режим - аккумулятивный для навигации из карты, синхронный для обычной навигации

**3. Позиционирование дочерних узлов (исправлено)**
- **Было**: дочерние узлы не следовали за движением родительских
- **Решение**: использование текущих позиций родителей вместо базовых

**4. Кнопка возврата без контекста (исправлено)**
- **Было**: возврат только к карте, без исходного слайда
- **Решение**: сохранение `lastSlideBeforeNavigation` и точное позиционирование

### 🖼️ Настройка фонового изображения

**Автоматический выбор изображения** (в порядке приоритета):
1. `resources/knowledge_map_bg.png` - кастомное изображение для карты
2. `resources/image_kubernetes.png` - стандартное изображение темы
3. Автогенерируемый градиент через ImageMagick при отсутствии изображений

**Создание фона через ImageMagick:**
```bash
convert -size 1152x580 \
    -define gradient:vector="0,0,1152,580" \
    gradient:"#0f172a-#1e293b" \
    \( -size 1152x580 pattern:hexagons -alpha set -channel A -evaluate set 10% \) \
    -compose over -composite \
    "resources/image_kubernetes.png"
```

### ⚙️ Логи и отладка

**Успешная интеграция:**
```bash
🗺️ Добавляем карту знаний
   🖼️ Используется существующее изображение: resources/image_kubernetes.png
   📋 Скопирован knowledge-map-new.js из .themes (настроено фоновое изображение)
   ✅ Карта знаний интегрирована
```

**Консоль отладки (для разработки):**
```javascript
[PROGRESS UPDATE] Navigation from map - accumulative mode
[MINDMAP] Создан узел: "admission-validating_admission_policy"
[MINDMAP] Checking node "admission-validating_admission_policy", revealed: true
[RETURN BUTTON] Navigated from map, showing return button
```

### 🎯 Рекомендации по разработке

**При добавлении новых возможностей:**
1. **Тестируйте оба режима прогресса** - аккумулятивный и синхронный
2. **Проверяйте согласованность ключей** - hex-карта vs mindmap vs localStorage
3. **Учитывайте состояние навигации** - ручная vs программная
4. **Добавляйте логирование** - для отладки сложных состояний
5. **Тестируйте через полный цикл** - открытие карты → навигация → возврат

**Архитектурные принципы:**
- **Неинвазивность** - система не мешает обычной навигации
- **Персистентность** - прогресс сохраняется между сессиями
- **Интуитивность** - кнопки появляются и исчезают логично
- **Производительность** - минимальные вычисления при обновлениях

**Карта знаний работает только в HTML-презентациях** и предоставляет студентам интерактивный способ отслеживания и навигации по концептам обучения.

## 🗺️ Система миникарты навигации

### 📋 Автоматическая генерация

Миникарта автоматически создается для HTML-презентаций с отслеживанием прогресса по секциям и интерактивной навигацией.

**Интеграция через build-presentation.sh:**
- Автоматический анализ `data-topic` атрибутов в слайдах
- Генерация `generated/minimap.css` и `generated/minimap.js`
- Подключение стилей и скриптов к HTML
- Поддержка localStorage для персистентности прогресса

**Визуальные возможности:**
- Прогресс-бары для каждой секции презентации
- Эмоджи и названия тем из `data-topic`
- Индикация текущей секции оранжевой рамкой
- Отметки о посещенных секциях зеленым цветом

**Технические требования:**
- Максимум 7 топиков для оптимальной читаемости
- Поддерживаемые эмоджи: 🏗️🐳🛡️🧪⚡🔧📊💼📝🚀⚙️🗄️🌐🔌🎨📱☁️📈🔄
- Формат разметки: `data-topic="🔧 Название темы"`
- Показывается только на слайдах типа `page_section`

## 🎨 Автоматическая генерация изображений

### 📸 Интеграция DALL-E 3

Build-presentation.sh поддерживает автоматическую генерацию изображений через OpenAI API:

**Синтаксис команд:**
```markdown
<!-- СОЗДАТЬ ИЗОБРАЖЕНИЕ: English prompt for AI generation -->
<!-- ДОБАВИТЬ ТЕКСТ В (x,y): наложение текста -->
<!-- ДОБАВИТЬ ТЕКСТ В (x,y) РАЗМЕР size: текст с размером -->
```

**Автоматические процессы:**
- Сканирование комментариев с промптами изображений
- Генерация через DALL-E 3 в формате 1792x1024 (16:9)
- Масштабирование до 412px высоты для слайдов
- Кэширование для экономии API вызовов
- Проверка орфографии и грамматики в текстах

**Поддержка наложения текста:**
- ImageMagick для точного позиционирования
- Автоматическая контрастная обводка
- Координатная система от -1 до +1 (0,0 = центр)
- Настраиваемые размеры шрифта

## 🛠️ Централизованная сборка

### 📋 Архитектура build-presentation.sh

**Единая точка входа** для сборки всех типов презентаций с автоматической интеграцией всех систем:

```bash
.themes/build-presentation.sh presentation.md
```

**Автоматические компоненты:**
1. **Mermaid диаграммы** - генерация SVG из markdown блоков
2. **Изображения DALL-E** - создание иллюстраций по промптам
3. **Миникарта навигации** - анализ тем и создание интерфейса
4. **Карта знаний** - интеграция концептов и прогресса
5. **HTTP-сервер** - автозапуск для тестирования
6. **Marp компиляция** - финальная генерация HTML

**Выходные форматы:**
- HTML с полной интерактивностью (миникарта + карта знаний + D3.js демо)
- Статичные версии для экспорта (PDF/PPTX совместимость)
- Автоматическая организация ресурсов в generated/

Система обеспечивает единообразие сборки и автоматическую интеграцию всех интерактивных компонентов презентаций.